(ns lein-autotest-notify.test.core
  ;(:use [lein-autotest-notify.core])
  (:use [clojure.test]))

(deftest replace-me ;; FIXME: write
  (is false "No tests have been written.")
  (is (= 1 1))
  (throw (Exception. "hello")))
